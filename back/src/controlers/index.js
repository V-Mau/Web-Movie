// *En este módulo va a estat las funciondes q se establece la LÓGICA DE NEGOCIO de cada una de las rutas
const testControlers = (req, res) => {

    res.status(200).send('Proximamente estarán disponible los datos de las pelis');
};


module.exports = {
    testControlers 
}